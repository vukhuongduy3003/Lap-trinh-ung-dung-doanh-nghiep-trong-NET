
@{
    ViewBag.Title = "Index3";
}

<h2>Index3</h2>
@{
    var users = Model as List<WebApplication.Models.User>;
    foreach (var user in users)
    {
        <h3>Id: @user.Id</h3>
        <p>Name: @user.Name</p>
        <p>Address: @user.Address</p>
        <p>Email: @user.Email</p>
    }
}
